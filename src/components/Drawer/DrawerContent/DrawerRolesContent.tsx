import { RoleType } from "../../../types/userTypes";

export default function DrawerRolesContent() {
  return (
    <div>{/* <h2 className="text-center font-Lato text-2xl">{board ? `Edit ${board.title}` : "Create New Graph"}</h2> */}</div>
  );
}
